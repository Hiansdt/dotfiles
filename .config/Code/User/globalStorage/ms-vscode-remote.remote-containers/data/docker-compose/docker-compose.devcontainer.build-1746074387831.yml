version: '3'

services:
  app:
    build:
      dockerfile: /tmp/devcontainercli-hiansdt/container-features/0.75.0-1746074381943/Dockerfile-with-features
      args:
        - _DEV_CONTAINERS_BASE_IMAGE=dev_container_auto_added_stage_label
        - _DEV_CONTAINERS_IMAGE_USER=node
        - _DEV_CONTAINERS_FEATURE_CONTENT_SOURCE=dev_container_feature_content_temp

